@using System.Activities.Expressions
@using BootstrapSupport
@model Object


   
        @using (Html.BeginForm())
        {
            <div class="row">
                <div class="span6">
                    @Html.ValidationSummary(true)
                    <fieldset class="form-horizontal">
                        <legend>Register your account</legend>
                        @foreach (var property in Model.VisibleProperties())
                        {
                            if (property.Name == "ShowTwitter" || property.Name == "ShowFacebook" || property.Name == "ShowFullName" || property.Name == "ShowEmail")
                            {

                            }
                            else
                            {
                                using(Html.ControlGroupFor(property.Name))
                                {
                                    @Html.Label(property.Name.ToSeparatedWords(), new { @class = "control-label" })
                                    <div class="controls">
                                        @Html.Editor(property.Name, new { @class = "input-xlarge" })
                                        @Html.ValidationMessage(property.Name, null, new { @class = "help-inline" })
                                    </div>
                                }
                            }
                         
                        }
                    </fieldset>
                </div>
                <div class="span6">
                    <fieldset class="form-horizontal">
                        <legend>What to show on your profile</legend>
                    
                        @foreach (var property in Model.VisibleProperties())
                        {
                            if (property.Name == "ShowTwitter" || property.Name == "ShowFacebook" || property.Name == "ShowFullName" || property.Name == "ShowEmail")
                            {
                                using(Html.ControlGroupFor(property.Name))
                                {
                                    @Html.Label(property.Name.ToSeparatedWords(), new { @class = "control-label" })
                                    <div class="controls">
                                        @Html.Editor(property.Name, new { @class = "input-xlarge" })
                                        @Html.ValidationMessage(property.Name, null, new { @class = "help-inline" })
                                    </div>
                                }
                            }
                            else
                            {
                           
                            }
                         
                        }
                    </fieldset>
                </div>
            </div>
            <div class="">
                <button id="btn-register" type="submit" class="btn btn-success">Register</button>
                @Html.ActionLink("Back", "Index","Public", null, new { @class = "btn " })
            </div>
        }
   


